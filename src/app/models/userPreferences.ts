import { siteCategory } from "./siteCategory";

export class userPreferences{
    /*
    userEmail!: string;
    //categoryRatings!: {categoryId: number, categoryName: string, rating: number}[];
    categoryRatings!: {category: siteCategory, rating: number}[];
    */
    categoryId!: number;
    categoryName!: string;
    categoryRating!: number;
}